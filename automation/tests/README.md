# Automated tests

Automated test code will be added after manual commands and expected outputs are
verified against the installed RouterOS and FRR versions. Tests must preserve raw
output and update `results/test-results.csv` without converting `not_run` into
`pass` unless the required evidence exists.
