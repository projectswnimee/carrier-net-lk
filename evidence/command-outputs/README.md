# Command outputs

This directory contains selected console output captured during the validated
build. Files are named by test ID and device. Values such as uptime, label
numbers and latency are observations from that run and can differ after a
restart without indicating a fault.

No file contains router credentials. Tests confirmed only interactively are
identified as operator-observed in `results/test-results.csv` rather than being
represented by reconstructed output.
